# Bundled Xiaohongshu relay runtime

This directory contains the Python runtime files required by the workbench.
The launcher uses these files first, so a fresh GitHub checkout does not need a
separate Codex Skill installation. A local `CODEX_HOME` Skill or explicit
`XHS_UPSTREAM_RUNNER` / `XHS_UPSTREAM_SCRAPER` path still takes precedence.
